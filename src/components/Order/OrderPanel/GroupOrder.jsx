import React from "react";
import styled from "styled-components";

const Container = styled.div`
  background: green;
  height: 64px;
  margin-right: 20px;
`;

const GroupOrder = props => <Container>Group Order</Container>;

export default GroupOrder;
